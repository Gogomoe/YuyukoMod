package demo.characters

import demo.CharacterColor
import demo.patches.CardColorEnum

class Reimu {
    companion object {
        val color = CharacterColor(CardColorEnum.REIMU_COLOR!!, "characters", 238f, 48f, 48f)
    }
}
